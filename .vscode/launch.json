{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Backend (FastAPI)",
            "type": "debugpy",
            "request": "launch",
            "module": "uvicorn",
            "args": [
                "main:app",
                "--reload"
            ],
            "cwd": "${workspaceFolder}/backend",
            "justMyCode": true
        }
    ]
}